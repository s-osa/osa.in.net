!!! html

%html
  %head
    %meta{ charset: 'utf-8' }
    %title= current_page.data.title || 'osa.in.net'
    = stylesheet_link_tag :site

  %body{ class: "#{page_classes}" }
    .wrapper
      .block
        %h1.site-name osa.in.net

      = yield
